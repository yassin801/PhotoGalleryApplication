<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.PhotoGalleryApplication" parent="android:Theme.Material.Light.NoActionBar" />

    <style name="Theme.AppCompat.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_screen</item>
        <item name="postSplashScreenTheme">@style/Theme.PhotoGalleryApplication</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/logo</item>
    </style>
</resources>
